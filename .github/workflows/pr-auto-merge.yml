name: PR Auto Merge

on:
  workflow_run:
    workflows: ["DPM Rebalance Engine Pipeline"]
    types: [completed]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    name: Auto Merge PR After Green CI
    if: github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Find associated open PR
        id: find-pr
        uses: actions/github-script@v7
        with:
          script: |
            const sha = context.payload.workflow_run.head_sha;
            const { data: prs } = await github.rest.repos.listPullRequestsAssociatedWithCommit({
              owner: context.repo.owner,
              repo: context.repo.repo,
              commit_sha: sha,
            });

            const pr = prs.find((item) => item.state === "open" && item.draft === false);
            if (!pr) {
              core.info("No open non-draft PR associated with this successful workflow run.");
              core.setOutput("number", "");
              return;
            }

            core.info(`Found PR #${pr.number}: ${pr.title}`);
            core.setOutput("number", String(pr.number));

      - name: Merge PR (squash)
        if: steps.find-pr.outputs.number != ''
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.find-pr.outputs.number }}
        run: |
          gh pr merge "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --squash --delete-branch
