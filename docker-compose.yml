version: '3.8'

services:
  dpm-rebalance-engine:
    build: 
      context: .
      dockerfile: Dockerfile
    image: dpm-rebalance-engine:latest
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=development
