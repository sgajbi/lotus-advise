[pytest]
testpaths =
    tests/unit
    tests/integration
    tests/e2e
markers =
    unit: Fast tests that run in isolation with mocked or in-memory dependencies.
    integration: Tests that validate interaction with external systems (for example live Postgres).
    e2e: End-to-end workflow tests covering higher-level API/user journeys.
addopts = --strict-markers
